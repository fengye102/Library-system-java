<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.library.mapper.BorrowMapper">

    <resultMap id="borrowMap" type="com.library.entity.Borrow">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="book_id" property="bookId"/>
        <result column="borrow_time" property="borrowTime"/>
        <result column="return_time" property="returnTime"/>
        <result column="due_time" property="dueTime"/>
        <result column="status" property="status"/>
        <association property="user" javaType="com.library.entity.User">
            <id column="u_id" property="id"/>
            <result column="username" property="username"/>
            <result column="realname" property="realname"/>
        </association>
        <association property="book" javaType="com.library.entity.Book">
            <id column="b_id" property="id"/>
            <result column="book_name" property="name"/>
            <result column="author" property="author"/>
        </association>
    </resultMap>

    <select id="findById" parameterType="Integer" resultMap="borrowMap">
        SELECT br.*,
               u.id AS u_id, u.username, u.realname,
               bk.id AS b_id, bk.name AS book_name, bk.author
        FROM borrow br
        LEFT JOIN `user` u  ON br.user_id = u.id
        LEFT JOIN book   bk ON br.book_id  = bk.id
        WHERE br.id = #{id}
    </select>

    <select id="findByUserId" parameterType="Integer" resultMap="borrowMap">
        SELECT br.*,
               u.id AS u_id, u.username, u.realname,
               bk.id AS b_id, bk.name AS book_name, bk.author
        FROM borrow br
        LEFT JOIN `user` u  ON br.user_id = u.id
        LEFT JOIN book   bk ON br.book_id  = bk.id
        WHERE br.user_id = #{userId}
        ORDER BY br.borrow_time DESC
    </select>

    <select id="findAll" resultMap="borrowMap">
        SELECT br.*,
               u.id AS u_id, u.username, u.realname,
               bk.id AS b_id, bk.name AS book_name, bk.author
        FROM borrow br
        LEFT JOIN `user` u  ON br.user_id = u.id
        LEFT JOIN book   bk ON br.book_id  = bk.id
        ORDER BY br.borrow_time DESC
    </select>

    <select id="findActiveBorrowByUserAndBook" resultMap="borrowMap">
        SELECT * FROM borrow
        WHERE user_id = #{param1} AND book_id = #{param2} AND status = 0
    </select>

    <insert id="insert" parameterType="com.library.entity.Borrow" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO borrow (user_id, book_id, borrow_time, due_time, status)
        VALUES (#{userId}, #{bookId}, NOW(), #{dueTime}, 0)
    </insert>

    <update id="returnBook" parameterType="Integer">
        UPDATE borrow SET status = 1, return_time = NOW() WHERE id = #{id}
    </update>

</mapper>
